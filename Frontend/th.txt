In frontend we follow the features based folder structure, as appllication have diff diff types of features like auth(login,logout,forgot pass) , ai(res upload etc), as app grows features grows\

as in total we have 4 layers
    UI
     (it includes folders like- component, pages)

    Hook-for mananging state and api layers
     (it include folder such as - hooks)

    State-to store data in frontend , user state (basic loggedin user ka data) and loading state(api se info tak aane ke beech ka samay)
     (it includes file like- auth.context.jsx or more)


    API-communicate with backend on behalf of frontend
     (and it include folder - Services - have a file auth.api.js) 


    // AI part

    On the behalf of the user -> it give you (self descrip, Res[pdf], job descrip)
    so in teen cheezo ko server dega ai(gemini LLM ) ko and report (JSON) generate hogi
    include - tech qs, behavioral qs, skills gaps, prep plans
    and report can be save in db( need schema)

    // for api go to the gemini docs 
    //  and choose model in such a way ki, it gives structured output (go to structure output )