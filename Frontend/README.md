# React 4 Layer Structure

## UI → what user sees
components and pages

## Hooks → for managing state and API layer
custom hooks

## State → global data (basically frotend ka jo data store hoga)
auth.context.jsx, ai.context.jsx

## API → server calls or (for communication with backend)
services/auth.api.js

# some othere imp things

## AXIOS -> jub frontend ko backend se communicate karna rehta hai